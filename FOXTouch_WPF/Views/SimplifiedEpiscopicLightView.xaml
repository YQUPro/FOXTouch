<UserControl 
    x:Class="FOXTouch_WPF.Views.SimplifiedEpiscopicLightView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:local="clr-namespace:FOXTouch_WPF.Views"
    Background="Transparent"
    HorizontalAlignment="Stretch"
    VerticalAlignment="Stretch"
    MinHeight="94"
    MinWidth="729"
    mc:Ignorable="d"
    >

    <Border 
        BorderThickness="10" 
        CornerRadius="10" 
        HorizontalAlignment="Stretch"
        VerticalAlignment="Stretch"
        BorderBrush="{StaticResource PrimaryBackgroundColourBrush}">
        <Grid
            Background="{StaticResource PrimaryBackgroundColourBrush}">
            <!-- Définir les colonnes et les lignes -->
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <!-- Image dans la case (0,0) -->
            <Image
                Grid.Column="0"
                Height="64"
                Width="64"
                Margin="0,0,10,0"
                Source="pack://application:,,,/FOXTouch_WPF;component/Resources/SimplifiedLightsView_EpiscopicLight.png" Stretch="Uniform"
                />
            <!-- Slider dans la case (1,0) -->
            <StackPanel
                Grid.Column="1"
                VerticalAlignment="Center" 
                HorizontalAlignment="Center"
                Background="Transparent"
                >
                <Slider 
                    Minimum="0"
                    Maximum="100" 
                    TickFrequency="25"
                    IsSnapToTickEnabled="True"
                    TickPlacement="Both"
                    Width="200"
                    Height="64"
                    Style="{StaticResource CustomSliderStyle}"
                    Value="25"/>
            </StackPanel>
            <!-- Text dans la case (2,0) -->
            <TextBlock
                Grid.Column="2"
                Margin="20,0,20,0"
                Text="XXX %" 
                TextAlignment="Center" 
                FontSize="32"
                FontWeight="Bold"
                Background="{StaticResource PrimaryBackgroundColourBrush}"
                Foreground="{StaticResource PrimaryForegroundColorBrush}"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Center"
                    />
            <!-- CheckBox 'Secteur Arrière' dans la case (3,0) -->
            <CheckBox 
                Grid.Column="3"
                Style="{StaticResource BaseLargeCheckBoxStyle}"
                >
                <CheckBox.Background>
                    <ImageBrush ImageSource="pack://application:,,,/FOXTouch_WPF;component/Resources/SectorialLight_BackSector.png" Stretch="Uniform"/>
                </CheckBox.Background>
            </CheckBox>
            <!-- CheckBox 'Secteur Droite' dans la case (4,0) -->
            <CheckBox 
                Grid.Column="4"
                Style="{StaticResource BaseLargeCheckBoxStyle}"
                >
                <CheckBox.Background>
                    <ImageBrush ImageSource="pack://application:,,,/FOXTouch_WPF;component/Resources/SectorialLight_RightSector.png" Stretch="Uniform"/>
                </CheckBox.Background>
            </CheckBox>
            <!-- CheckBox 'Secteur Avant' dans la case (5,0) -->
            <CheckBox 
                Grid.Column="5"
                Style="{StaticResource BaseLargeCheckBoxStyle}"
                >
                <CheckBox.Background>
                    <ImageBrush ImageSource="pack://application:,,,/FOXTouch_WPF;component/Resources/SectorialLight_FrontSector.png" Stretch="Uniform"/>
                </CheckBox.Background>
            </CheckBox>
            <!-- CheckBox 'Secteur Gauche' dans la case (6,0) -->
            <CheckBox 
                Grid.Column="6"
                Style="{StaticResource BaseLargeCheckBoxStyle}"
                >
                <CheckBox.Background>
                    <ImageBrush ImageSource="pack://application:,,,/FOXTouch_WPF;component/Resources/SectorialLight_LeftSector.png" Stretch="Uniform"/>
                </CheckBox.Background>
            </CheckBox>
        </Grid>
    </Border>
</UserControl>